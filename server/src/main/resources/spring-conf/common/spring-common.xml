<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:tx="http://www.springframework.org/schema/tx" xmlns:aop="http://www.springframework.org/schema/aop"
	   xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:cache="http://www.springframework.org/schema/cache"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-4.0.xsd


		http://www.springframework.org/schema/tx
		http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
		http://www.springframework.org/schema/cache
  		http://www.springframework.org/schema/cache/spring-cache-4.0.xsd
		http://www.springframework.org/schema/aop
		http://www.springframework.org/schema/aop/spring-aop-4.0.xsd
		http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

	<bean id="jdbcTemplateSupportDao" class="com.hayden.hap.common.db.orm.jdbc.JdbcTemplateSupportDao">
		<property name="shardingRouter" value="#{'${jdbc_base_mycat.dbType}' == 'dm'?null:shardingRouter}"  />
		<property name="defaultDataSourceId" value="dataSource" />
		<property name="multTaskMode" value="false" /><!-- 多数据源情况下是否开启多线程模式,默认为true(开启) -->
	</bean>
	<bean id="shardingRouter" class="com.hayden.hap.common.db.sharding.route.ShardingRouter"/>
	
	<!-- 线程池配置 -->
	<bean id="threadPoolTsakExecutor"  class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
		<property name="corePoolSize" value="${threadPool.corePoolSize}" />
		<property name="keepAliveSeconds" value="${threadPool.keepAliveSeconds}" />
		<property name="maxPoolSize" value="${threadPool.maxPoolSize}" />
		<property name="queueCapacity" value="${threadPool.queueCapacity}" />
		<property name="rejectedExecutionHandler">
			<bean
					class="java.util.concurrent.ThreadPoolExecutor$CallerRunsPolicy" />
		</property>
	</bean>

	<!-- 事务配置 -->
	<!-- (事务管理)transaction manager, use JtaTransactionManager for global tx -->
	<bean id="transactionManager" class="com.hayden.hap.common.db.sharding.transaction.ShardingTransactionManager">
		<property name="synchronizationManager" >
			<bean class = "com.hayden.hap.common.db.sharding.transaction.DefaultSynchronizationManager" />
		</property>
		<property name="defaultDataSourceId" value="dataSource" />
		<property name="defaultDataSource" ref="dataSource" />
	</bean>

	<!-- enable transaction demarcation with annotations -->
	<tx:annotation-driven transaction-manager="transactionManager" order="0"/>

	<!-- 配置事务特性 -->
	<tx:advice id="txAdvice"
			   transaction-manager="transactionManager">
		<tx:attributes>
			<!--  <tx:method name="get*" propagation="REQUIRED" isolation="READ_COMMITTED" read-only="true" rollback-for="Exception"/>
             <tx:method name="query*" propagation="REQUIRED" isolation="READ_COMMITTED" read-only="true" rollback-for="Exception"/>
             <tx:method name="find*" propagation="REQUIRED" isolation="READ_COMMITTED" read-only="true" rollback-for="Exception"/>
             <tx:method name="executeQuery*" propagation="REQUIRED" isolation="READ_COMMITTED" read-only="true" rollback-for="Exception"/>-->
			<tx:method name="*_NotSupported" propagation="NOT_SUPPORTED" isolation="READ_COMMITTED" read-only="true"/>
			<tx:method name="*_RequiresNew" propagation="REQUIRES_NEW" isolation="READ_COMMITTED" rollback-for="Exception"/>
			<!--<tx:method name="*" propagation="REQUIRED" isolation="READ_COMMITTED" rollback-for="Exception"/>  -->
		</tx:attributes>
	</tx:advice>

	<!-- 配置哪些类的方法需要进行事务管理 -->
	<aop:config>
		<aop:pointcut id="allManagerMethod"
					  expression="execution(* (com.hayden..service.*+).*(..)  )" />
		<aop:advisor advice-ref="txAdvice"
					 pointcut-ref="allManagerMethod" order="1" />
	</aop:config>

	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix">
			<value>/WEB-INF/views/</value>
		</property>
		<property name="suffix">
			<value>.html</value>
		</property>
	</bean>
	<!-- 配置静态资源 -->
	<mvc:default-servlet-handler/>

	<bean id="applicationContextUtils" class="com.hayden.hap.common.utils.ApplicationContextUtils" />

</beans>