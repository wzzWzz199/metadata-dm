# hap-metadata-client

This is a lightweight Vue 3 + Vite shell for the metadata management UI. Use `npm install` to pull
node modules, `npm run dev` for hot-module dev server, and `npm run build` to emit a production-ready
bundle under `dist/`.
